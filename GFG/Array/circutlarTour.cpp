int circularTour(pair petrolPump[], int n)
{
	for(int i=0; i<n; i++)
		petrolPump[i].first-=petrolPump[i].second;
	
	int prev=0, sum=0, idx=0;
	for(int i=0; i<0; i++)
	{
		sum+=A[i];
		if(prev>sum && sum<0)
		{
			
		}
	}
}
